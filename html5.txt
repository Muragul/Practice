<script src="script.js">
var a='asd';
alert(a); - создает баннер на странице
все условные операторы работают как в с++

особенности языка
let a=0;b=false;
alert(a==b); - true, так как сравнивает по значению
alert(a===b); - false, так как сравнивает по значению и по типу данных

html:
<button class='btn' id='forButtons' onclick='AddButton()'>

JavaScript:
function AddButton(){
var forButtons=document.GetElementById('forButtons');
var btn = document.CreateElement('button');
btn.className='btn btn-light';
btn.innerHTML='new button';
btn.onclick = function(){ здесь функции не нужно давать имя
var text=document.CreateTextNote('asd');
var li=document.CreateElement(li);
var list=document.GetElementById('list');
li.AppendChild(text);
list.AppendChild(li);
}
forButtons.AppendChild(btn);
}

fuction forDel(){
var div=document.GetElementById(deldiv);
btn=document.CreateElement('Button');
btn.className
}